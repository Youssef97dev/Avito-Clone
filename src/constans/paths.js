const HOME_ROUTE = "/";

const PRODUCTS_ROUTE = "/products/:category";

const SIGN_IN = "/signin";

const SIGN_UP = "/signup";

const paths = {HOME_ROUTE, PRODUCTS_ROUTE, SIGN_IN, SIGN_UP};

export default paths;